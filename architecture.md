mazer-core = build the core library of the langauge with parser, interpreter 

mazer-dbg = general purpose debugging tools

mazer-cli = cli interface for the library i.e. installable

mazer-macros = helpful macros depended upon by core

